FROM node:slim

RUN apt-get update && apt-get install -y curl
RUN echo "which yarn: $(which yarn)"
RUN npm install -g yarn

WORKDIR /app

COPY package*.json .

RUN yarn install --production=true --frozen-lockfile